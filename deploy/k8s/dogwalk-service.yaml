apiVersion: apps/v1
kind: Deployment
metadata:
  name: dogwalk-service
  namespace: patch-pet
  labels:
    app: dogwalk-service
    app.kubernetes.io/name: dogwalk-service
    app.kubernetes.io/part-of: patch-pet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dogwalk-service
  template:
    metadata:
      labels:
        app: dogwalk-service
    spec:
      containers:
        - name: dogwalk-service
          image: patch-pet/dogwalk-service:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: APP_ENV
              value: "production"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
